var main = function (input) {
  var fah = 9 / 5;
  var constant = 32;
  CELFAH = input * fah + constant;
  var myOutputValue = `The temperature is ${CELFAH} degrees Fahrenheit.`;
  return myOutputValue;
};
